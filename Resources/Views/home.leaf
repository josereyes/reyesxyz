#set("about") {
<div class="columns">
  #embed("emptycolumn")
  <div class="column is-10">
    <section class = "hero is-fullheight is-bold">
      <div class="hero-body">
        <div class="container">
          #embed("name")
          #embed("subtitle")
          #embed("social")
        </div>
      </div>
      #embed("footer")
    </section>
  </div>
  #embed("emptycolumn")
</div>
}

#embed("master")
